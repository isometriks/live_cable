<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "LiveCable" %></title>
    <meta name="csrf-token" content="<%= form_authenticity_token %>">

    <%# Built from config/importmap.rb plus the gem's own pins, so every module
        resolves to a local Propshaft asset rather than a CDN. %>
    <%= javascript_inline_importmap_tag %>

    <script type="module">
      import { Application } from "@hotwired/stimulus"
      import LiveController from "@isometriks/live_cable/controller"
      import "@isometriks/live_cable"

      const application = Application.start()
      application.register("live", LiveController)
    </script>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
